<theme>
    <include>./helpsystem.xml</include>
    <include>./status.xml</include>
    <!-- section-strip highlight state MUST precede section-strip.xml: ES-DE resolves variables
         in declaration order, so each var must be defined before the element that uses it
         (and an override must follow its default). _default first, then per-collection override. -->
    <include>./section-state/_default.xml</include>
    <include>./section-state/${system.theme}.xml</include>
    <!-- Steam-style persistent section nav (consumes the section-state vars above) -->
    <include>./section-strip.xml</include>
    <!-- official ES-DE per-system metadata: ${systemName} (short name) + ${systemColor} (tint); full coverage, _default fallback -->
    <include>./../system-metadata/_default.xml</include>
    <include>./../system-metadata/${system.theme}.xml</include>
    <!-- targeted overrides for ES-DE short names that collapse to the manufacturer (e.g. nes -> NES) -->
    <include>./name-overrides/${system.theme}.xml</include>
    <!-- collection hero accent color (after metadata so it overrides the grey _default) -->
    <include>./section-accent/${system.theme}.xml</include>
    <view name="system">
        <!-- fallback base gradient (shown only if a system has no art) -->
        <image name="heroBg">
            <pos>0 0</pos><size>1 1</size>
            <path>./../assets/ui/hero-bg.svg</path>
            <zIndex>1</zIndex>
        </image>
        <!-- per-platform art, full-screen (cover-cropped to fill) -->
        <image name="heroArt">
            <pos>0 0</pos><origin>0 0</origin>
            <cropSize>1 1</cropSize><cropPos>0.5 0.5</cropPos>
            <path>./../systems/art/${system.theme}.jpg</path>
            <default>./../assets/ui/art-placeholder.svg</default>
            <zIndex>2</zIndex>
        </image>
        <!-- (mood colour tint removed for the art variant: art shows in its natural colours) -->
        <!-- (railScrim removed for the art variant: it was bottom-heavy and stacked with the
             footer, making the bottom darker than the top. The uniform railShelf below replaces it.) -->
        <!-- left scrim for text legibility -->
        <image name="heroScrim">
            <pos>0 0</pos><size>0.62 1</size>
            <path>./../assets/ui/scrim-left.svg</path>
            <zIndex>8</zIndex>
        </image>
        <!-- system name + count -->
        <!-- primary: official short name (${systemName}); falls back to full name for un-curated systems -->
        <text name="sysName">
            <pos>0.05 0.55</pos><size>0.60 0.095</size>
            <text>${systemName}</text>
            <container>false</container>
            <fontPath>${fontDisplay}</fontPath><fontSize>${fsHero}</fontSize>
            <color>${cText}</color><zIndex>10</zIndex>
        </text>
        <!-- secondary: manufacturer (non-redundant with the system name; ${systemManufacturer} from metadata) -->
        <text name="sysFull">
            <pos>0.05 0.645</pos><size>0.62 0.04</size>
            <text>${systemManufacturer}</text>
            <container>false</container>
            <fontPath>${fontSemiBold}</fontPath><fontSize>${fsLabel}</fontSize>
            <color>${cText2}</color><zIndex>10</zIndex>
        </text>
        <text name="sysCount">
            <pos>0.05 0.70</pos><size>0.5 0.05</size>
            <systemdata>gamecount</systemdata>
            <fontPath>${fontSemiBold}</fontPath><fontSize>${fsCount}</fontSize>
            <color>${cText2}</color><zIndex>10</zIndex>
        </text>
        <!-- dark shelf behind the icon rail so the outline icons stay legible over busy art.
             zIndex 9 = above the scrims (<=8) but below the name text (10) and the rail (40). -->
        <image name="railShelf">
            <pos>0 0.52</pos><size>1 0.405</size>
            <path>./../assets/ui/rail-shelf.svg</path>
            <zIndex>9</zIndex>
        </image>
        <!-- bottom rail of system icons (kept above the help footer at y 0.925) -->
        <carousel name="rail">
            <pos>0 0.75</pos><size>1 0.15</size>
            <type>horizontal</type>
            <staticImage>./../system-controllers-outline/${system.theme}.svg</staticImage>
            <defaultImage>./../assets/icons/fallback.svg</defaultImage>
            <maxItemCount>9</maxItemCount>
            <itemSize>0.085 0.12</itemSize>
            <itemScale>1.25</itemScale>
            <!-- big-screen focus: fade non-selected icons so the selected (white, scaled) pops -->
            <unfocusedItemOpacity>0.5</unfocusedItemOpacity>
            <imageColor>e8eef5ff</imageColor>
            <imageSelectedColor>ffffffff</imageSelectedColor>
            <color>00000000</color>
            <text>${system.fullName}</text>
            <textColor>00000000</textColor>
            <zIndex>40</zIndex>
        </carousel>
    </view>
</theme>

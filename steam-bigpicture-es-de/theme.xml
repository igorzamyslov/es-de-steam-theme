<!-- system icons: ./system-controllers-outline/${system.theme}.svg ; logos: ./system-logos/system-logo-white/${system.theme}.svg (color: ./system-logos/system-logo-color/${system.theme}.svg) ; metadata: ./system-metadata/${system.theme}.xml -->
<theme>
    <include>./_inc/fonts.xml</include>
    <include>./_inc/colors.xml</include>

    <variant name="heroNeonGrid">
        <include>./_inc/system_hero_neon.xml</include>
        <include>./_inc/gamelist_grid.xml</include>
    </variant>
    <variant name="heroNeonList">
        <include>./_inc/system_hero_neon.xml</include>
        <include>./_inc/gamelist_list.xml</include>
    </variant>
    <variant name="heroArtGrid">
        <include>./_inc/system_hero_art.xml</include>
        <include>./_inc/gamelist_grid.xml</include>
    </variant>
    <variant name="heroArtList">
        <include>./_inc/system_hero_art.xml</include>
        <include>./_inc/gamelist_list.xml</include>
    </variant>
    <variant name="noHeroGrid">
        <include>./_inc/system_nohero.xml</include>
        <include>./_inc/gamelist_grid.xml</include>
    </variant>
    <variant name="noHeroList">
        <include>./_inc/system_nohero.xml</include>
        <include>./_inc/gamelist_list.xml</include>
    </variant>

    <!-- Aspect-ratio reflow overrides are deferred: they must be variant-scoped, because a
         global override block leaks a second primary element (e.g. the no-hero grid into a
         hero variant, or the rail carousel into the no-hero variant) and ES-DE then skips
         the real primary ("Multiple primary components defined"). The base layout uses
         normalized coordinates so it still scales to 16:10 / 4:3; bespoke per-variant
         reflow is a future pass done with <aspectRatio> nested inside each <variant>. -->
</theme>

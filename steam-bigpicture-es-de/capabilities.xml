<!-- Steam Big Picture for ES-DE -->
<themeCapabilities>
    <themeName>Steam Big Picture</themeName>

    <aspectRatio>16:9</aspectRatio>
    <aspectRatio>16:10</aspectRatio>
    <aspectRatio>4:3</aspectRatio>
    <aspectRatio>5:4</aspectRatio>
    <aspectRatio>19.5:9</aspectRatio>
    <aspectRatio>20:9</aspectRatio>
    <aspectRatio>21:9</aspectRatio>

    <fontSize>medium</fontSize>
    <fontSize>large</fontSize>
    <fontSize>x-large</fontSize>

    <!-- The colorScheme axis is repurposed as a detail-panel MEDIA selector (all share one dark
         palette). First entry is the default: Marquee, then video. -->
    <colorScheme name="marqueeVideo">
        <label language="en_US">Marquee → Video</label>
    </colorScheme>
    <colorScheme name="screenshotVideo">
        <label language="en_US">Screenshot → Video</label>
    </colorScheme>
    <colorScheme name="marqueeStill">
        <label language="en_US">Marquee (no video)</label>
    </colorScheme>
    <colorScheme name="screenshotStill">
        <label language="en_US">Screenshot (no video)</label>
    </colorScheme>

    <transitions name="steam">
        <label language="en_US">Steam</label>
        <selectable>true</selectable>
        <systemToSystem>instant</systemToSystem>
        <systemToGamelist>slide</systemToGamelist>
        <gamelistToGamelist>instant</gamelistToGamelist>
        <gamelistToSystem>slide</gamelistToSystem>
        <startupToSystem>fade</startupToSystem>
        <startupToGamelist>fade</startupToGamelist>
    </transitions>

    <!-- 2 system heroes x 2 gamelist layouts = 4 variants (detail media moved to the colorScheme axis).
         First entry is the default: Hero (neon) · Grid. -->
    <variant name="heroNeonGrid"><label language="en_US">Hero (neon) · Grid</label><selectable>true</selectable></variant>
    <variant name="heroNeonList"><label language="en_US">Hero (neon) · List</label><selectable>true</selectable></variant>
    <variant name="heroArtGrid"><label language="en_US">Hero (art) · Grid</label><selectable>true</selectable></variant>
    <variant name="heroArtList"><label language="en_US">Hero (art) · List</label><selectable>true</selectable></variant>
</themeCapabilities>
